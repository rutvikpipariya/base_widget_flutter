# Base Widget Flutter

Base Widget Flutter is a Flutter package that provides reusable base widgets to simplify UI development.

## Features
- Predefined widgets for common UI patterns
- Customizable and easy to use
- Reduces boilerplate code in Flutter apps

## Installation
Add this package to your `pubspec.yaml`:
```yaml
dependencies:
  base_widget_flutter: ^0.0.1